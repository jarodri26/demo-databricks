bundle:
  name: demo_job

include:
  - resources/*.yml
  - conf/**/*.yml

targets:
  # The 'dev' target, for development purposes. This target is the default.
  dev:
    mode: development
    default: true
    workspace:
      host: ip-10-53-219-166.ec2.internal

  # The 'prd' target, used for production deployment.
  prd:
    # We use 'mode: production' to indicate this is a production deployment.
    # Doing so enables strict verification of the settings below.
    mode: production
    workspace:
      host: ip-10-53-219-166.ec2.internal
      root_path: /Workspace/Users/jacarodriguez26@gmail.com/.bundle/${bundle.name}/${bundle.target}
     
